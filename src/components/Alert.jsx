import React from "react";

const Alert = () => {
    return(
        <div className="alert alert-warning" role="alert">
            Fields cnnot be left empty
        </div>
    );
};

export default Alert;